package dev.csshortcourse.shared.util

import android.widget.ImageView

// This is an extension to load image from a url
// into an ImageView using the Glide Library
fun ImageView.load(url: String?) {
    // todo: load image using Glide library
}